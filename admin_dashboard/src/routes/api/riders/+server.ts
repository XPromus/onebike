import type { PutRiderDto } from "$lib/types/server/rider.types";
import type { RequestHandler } from "@sveltejs/kit";

// export const PUT: RequestHandler = async ({
//     request, fetch
// }) => {
//     const payload: PutRiderDto = await request.json();

//     try {
//         const result = await 
//     }
// }
